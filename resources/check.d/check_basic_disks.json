{
  "checks": {
    "check_basic_disks": {
      "command": "${PLUGINS_DIR}/basic/check-disk.rb -w 80 -c 90 -d -L /host/ -t ext4 -i /host/boot",
      "handlers": [
        "mailer"
      ],
      "subscribers": [
        "basic"
      ],
      "interval": 60
    }
  }
}
